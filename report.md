# aheui_project

## 1. 기획 의도

버킷리스트 성취 및 스택 메모리 구조에 대한 이해도 향상.
가상 머신 구조를 직접 구현하며 인터프리터 동작 원리를 체화하는 것이 목표.

---

## 2. 개발 일지 (2026-05-24)

### 아희 에디터 그리드 구현

* 2D grid 기반 코드 에디터 구현
* cx, cy 기반 커서 시스템 설계
* hidden_input을 통한 IME 입력 처리 구조 채택

---

### 포커싱 문제 해결

문제:

* 에디터 클릭 및 키 입력 시 focus가 풀리며 한글 입력이 끊기는 현상 발생

원인:

* DOM focus가 editor와 hidden_input 사이에서 흔들림
* IME 입력은 input 요소 포커스에 종속됨

해결:

* 입력 포커스를 hidden_input으로 고정
* mousedown 이벤트에서 focus 강제 유지
* render 함수 마지막에도 focus 보장 처리 (fallback)

결과:

* IME 입력 안정화
* 커서 이동과 입력 동기화 정상화

---

## 3. 개발 일지 (2026-05-25)

### 스택 메모리 시스템 구조 확장

* 기존 단순 label 기반 스택 구조에서 종성(JONG) 기반 메모리 구조로 전환
* 각 종성을 독립적인 stack memory slot으로 사용하도록 설계
* Map 기반 구조로 스택을 레지스터 파일처럼 관리

구조:

* JONG[i] → stack[i]
* active 변수로 현재 조작 대상 스택 결정

---

### 스택 선택 명령(ㅅ) 로직 설계

* 종성 값을 기반으로 active stack 변경 기능 구현
* 기존 문자열 label 방식 제거 후 유니코드 기반 직접 매핑 적용

동작:

* ㅅ + 종성 → 해당 종성 스택으로 전환

---

### 입출력 명령(ㅂ) VM 연결

* ㅂ + 종성 조합을 이용한 입력 시스템 구현
* 종성 'ㅇ' → 숫자 입력 처리
* 종성 'ㅎ' → 문자 입력 처리 (charCode 변환)

개선:

* window.push 제거
* VM 내부 push 함수로 완전 통합

---

### VM 실행 구조 안정화

* IP 기반 2D 좌표 실행 구조 유지
* 실행 흐름: fetch → decode → execute → move IP
* 실행 결과에 따른 방향 제어 구조 구현

---

### 실행 위치(IP) 시각화 시스템 구현

* 현재 실행 중인 좌표(IP)를 에디터 상에서 실시간 하이라이트하도록 구현
* 단일 강조에서 확장하여 execution trail(잔상) 구조 추가
* 최근 실행 경로 2~3칸을 점진적으로 흐려지는 형태로 표시

구조:

* ip → 현재 실행 위치
* ipTrail[] → 최근 실행 기록 저장
* render 단계에서 trail index 기반 class 적용

효과:

* VM 실행 흐름을 시각적으로 추적 가능
* 코드 진행 방향과 루프 구조 디버깅 난이도 감소

---

### 실행 루프 및 렌더 동기화 개선

문제:

* VM은 정상 실행되지만 UI 하이라이트가 이동하지 않는 현상 발생

원인:

* interpreter.js 내부 ip 상태와 editor.js 렌더 상태가 분리됨
* render 시점과 VM state 업데이트 시점 불일치

해결:

* window.ip 기반 전역 상태 브릿지 구성
* step() 이후 render() 강제 호출 구조 적용
* renderStack()와 동기화하여 VM 상태를 실시간 반영

결과:

* VM 실행 상태와 UI 상태 완전 동기화
* 인터프리터와 디버거 패널 간 상태 불일치 제거

---

### 스택 디버거 UI 완성
- renderStack 함수 구현으로 VM 상태 시각화
- 전체 스택을 실시간으로 출력하는 디버깅 패널 구성
- active stack 하이라이트 적용
- 최근 10개 요소만 표시하여 가독성 확보

효과:

* VM 내부 상태를 외부 UI로 완전히 관측 가능
* 메모리 흐름 디버깅 효율 증가

---

### 스택 패널 UX 개선

* 스택 패널 기본 상태를 collapsed 구조로 변경
* 필요한 스택만 펼쳐 확인할 수 있도록 토글 UI 구현
* stack size 기반 헤더 표시 추가

개선:

* 대량 스택 사용 시 가독성 향상
* 메모리 구조 관측 효율 증가

---

### VM 실행 초기화 구조 추가

* 실행 버튼 클릭 시 스택 메모리 초기화 로직 추가
* 이전 실행 상태가 다음 실행에 영향을 주지 않도록 수정
* IP trail 및 active stack 상태 초기화 포함

효과:

* 실행 간 상태 오염 제거
* VM 재실행 안정성 확보

---

### UI / VM 분리 구조 확립

* editor.js: 입력 + 렌더링 전담
* interpreter.js: VM 실행 전담
* window 객체를 통한 상태 브릿지 구성

결과:

* UI와 VM이 독립적으로 동작하면서도 실시간 동기화 가능

---

### 전체 구조 고도화 결과

* 단순 아희 에디터 → 실행 가능한 VM으로 확장 완료
* 스택 메모리 / IP / 디버거 UI / 실행 루프 통합 구조 완성

최종 결과:

* IME 입력 안정화
* 실시간 실행 시각화 구현
* 스택 메모리 디버거 완성
* VM 상태 실시간 추적 가능
* 인터프리터 구조와 UI 구조 완전 분리 성공

현재 상태:

* 단순 텍스트 에디터 수준을 넘어
* 실제 난해한 프로그래밍 언어 실행 환경(VM) 구조를 갖춘 단계까지 도달

---

## 4. 개발 일지 (2026-05-28)

### 에디터 셀 선택 시스템 구현

문제:

* 기존 에디터는 키보드 기반 커서 이동만 가능했으며, 마우스를 통한 직접 위치 선택이 불가능하였다.

원인:

* 렌더링된 grid cell과 내부 커서 좌표(cx, cy)가 연결되어 있지 않았음
* 클릭 이벤트는 존재했지만 실제 좌표 계산 및 상태 반영 로직이 구현되지 않은 상태였음

해결:

* 각 cell에 dataset.x, dataset.y 형태로 좌표 메타데이터 부여
* editor 영역에서 이벤트 위임 방식(event delegation) 적용
* 클릭된 cell의 좌표를 cx, cy에 직접 반영하도록 수정

구조:

* cell.dataset.x
* cell.dataset.y
* closest('.cell') 기반 선택 처리

결과:

* 마우스를 이용한 실시간 커서 이동 가능
* 에디터 조작 직관성 향상
* 이후 드래그 선택 및 블록 편집 기능 확장 기반 마련

---

### 출력 명령(ㅁ) 구현 중 저장공간 참조 문제 해결

문제:

* 출력 명령 `ㅁ` 실행 시 출력은 정상적으로 수행되었으나, 스택 내부 값이 제거되지 않는 현상 발생

원인:

* 저장공간 배열을 복사한 뒤 `pop()`을 수행하고 있었음
* 실제 VM 내부 상태가 아닌 복사본에 대해 연산이 수행되고 있었음

해결:

* 저장공간을 직접 참조하는 구조로 수정
* `stacks.get(window.active)` 기반으로 현재 활성 스택을 직접 조작하도록 변경

결과:

* `pop()` 결과가 실제 VM 상태에 정상 반영
* 출력 이후 스택 상태 일관성 확보
* 메모리 상태 디버깅 정확도 향상

---

### 입력 명령(ㅂ) 기능 확장

* 숫자 입력 기능 추가
* 종성 기반 입력 분기 구조 구현

동작:

* 종성 `ㅇ` → 숫자 입력 처리
* 종성 `ㅎ` → 문자 입력 처리 (charCode 변환)

개선:

* hidden_input 기반 입력 버퍼와 VM push 구조 완전 연결
* VM 내부 push 함수를 통해 저장공간에 직접 반영되도록 구조 통합

결과:

* 입출력 명령 체계 완성
* 실제 프로그램 실행 가능한 수준의 VM 입출력 구조 확보

---

### VM 구조 발전 결과

현재 인터프리터는 단순 코드 실행기를 넘어 다음 요소를 모두 포함하는 구조로 발전하였다.

구성 요소:

* 2D Grid 기반 코드 에디터
* IME 안정화 입력 시스템
* 종성 기반 다중 스택 메모리 구조
* 실시간 실행 위치(IP) 시각화
* execution trail 기반 흐름 추적
* 스택 디버거 UI
* VM 상태 실시간 동기화 구조
* 입출력 명령 시스템

현재 상태:

* 단순 텍스트 처리 수준을 넘어
* 실제 가상 머신(VM) 구조와 디버거 구조를 함께 갖춘 인터프리터 단계에 도달
* 난해한 프로그래밍 언어의 실행 흐름과 메모리 구조를 시각적으로 관측 가능한 상태 확보

---

## 5. 개발 일지 (2026-05-30)

### 방향 처리 구조 재배치 및 연산 명령 구현

문제:

* 연산 명령 실행 과정에서 `pop()` 이후 스택 요소 부족 시 이동 방향을 반전시키는 로직이 존재하였으나, 중성(방향 처리) 탐색 로직이 초성(명령 처리)보다 아래에 위치해 코드 흐름 추적이 어려웠음

원인:

* 방향 결정과 명령 실행이 분리된 순서로 배치되어 있었음
* 스택 언더플로우 상황에서 방향 전환 처리 흐름이 직관적이지 않았음

해결:

* 중성 탐색 로직을 상단으로 이동하여 방향 처리 우선 구조로 재배치
* 실행 흐름을
  `방향 결정 → 명령 실행`
  순서로 정리
* 추가 연산 명령 구현:

  * ㄷ
  * ㄸ
  * ㅌ
  * ㄴ
  * ㄹ

결과:

* VM 실행 흐름 가독성 향상
* 방향 기반 제어 흐름 구조 안정화
* 스택 부족 상황 처리 디버깅 난이도 감소
* 이후 명령 추가 및 유지보수 효율 증가

---

### 실행 제어 시스템 구축

문제:

* 테스트 과정에서 무한 루프가 발생할 경우 인터프리터 실행을 중단할 방법이 존재하지 않았음
* 브라우저가 지속적으로 점유되며 디버깅 효율이 크게 저하됨

원인:

* VM이 단일 실행 루프만을 기반으로 동작하고 있었으며 실행 상태(state) 개념이 존재하지 않았음

해결:

* 실행 버튼 추가
* 일시정지 버튼 추가
* 재개 버튼 추가
* 초기화 버튼 추가

구조:

* running 상태
* paused 상태
* reset 상태

형태로 VM 실행 흐름을 제어할 수 있도록 기반 설계

결과:

* 무한 루프 상황 대응 가능
* 테스트 반복 안정성 향상
* 인터프리터 제어 가능성 확보
* 단순 실행기 구조에서 상태 기반 VM 구조로 발전

추후 개선 예정:

* 현재 VM 상태에 따라 버튼 활성화/비활성화 처리 예정
* 실행 상태와 UI 상태를 동기화하여 잘못된 조작 방지 예정

---

### 출력 결과 패널(Output Panel) 구현

문제:

* 실행 결과를 확인하기 위해 매번 브라우저 개발자 도구(F12)를 사용해야 했음
* 출력 확인 과정이 개발 흐름을 지속적으로 끊고 있었음

원인:

* VM 출력 결과가 console 기반으로만 출력되고 있었으며 별도의 사용자 출력 인터페이스가 존재하지 않았음

해결:

* 별도의 출력 결과 창(Output Panel) 구현
* VM 출력 스트림을 UI와 직접 연결

결과:

* 실행 결과 실시간 확인 가능
* 테스트 및 디버깅 속도 향상
* 사용자 접근성 개선
* 자체 입출력 시스템 기반 확보

구조적 의미:

* 단순 콘솔 기반 인터프리터에서
* 독립적인 UI 입출력 환경을 가진 웹 기반 VM 구조로 발전

향후 확장 가능성:

* 출력 로그 누적 기능
* 에러 출력 분리
* stdin 입력 패널
* 디버그 로그 토글 기능
* 실행 시간 및 상태 표시 시스템

---

### 전체 구조 발전 결과

현재 인터프리터는 다음 요소를 포함하는 구조까지 발전하였다.

구성 요소:

* 2D Grid 기반 코드 에디터
* IME 안정화 입력 시스템
* 종성 기반 다중 스택 메모리 구조
* 방향 기반 실행 흐름 제어
* 실시간 실행 위치(IP) 시각화
* execution trail 기반 흐름 추적
* 스택 디버거 UI
* VM 상태 실시간 동기화 구조
* 입출력 명령 시스템
* 실행 상태 제어 시스템
* 출력 결과 패널

현재 상태:

* 단순 텍스트 에디터 수준을 넘어
* 실제 난해한 프로그래밍 언어 실행 환경과 디버거 구조를 함께 갖춘 웹 기반 VM 단계에 도달
* 실행 흐름, 메모리 상태, 출력 결과를 모두 실시간으로 관측 가능한 상태 확보

## 6. 개발 일지 (2026-06-02)

### 비한글 문자 무시 처리 구조 적용

문제:

* 기존 인터프리터는 한글 음절 범위(`가~힣`)가 아닌 문자를 만날 경우 실행 흐름이 중단되는 구조였음
* 공백, ASCII 문자, 주석성 문자열 등을 코드 영역에 자유롭게 배치할 수 없었음

원인:

* VM이 비한글 문자를 예외 상황처럼 처리하고 있었음
* 실행 불가능 문자에 대한 “skip” 개념이 존재하지 않았음

해결:

* 한글 음절 범위가 아닌 문자는 명령으로 해석하지 않도록 수정
* 비한글 문자 발견 시:

  * 명령 실행 생략
  * 현재 방향 유지
  * 다음 위치로 IP 이동

구조:

* `isHangul()` 기반 범위 판별 구조 적용
* `move()` 기반 이동 처리 분리 예정

결과:

* 공백 및 일반 문자열을 자유롭게 배치 가능
* 주석성 텍스트 및 시각적 코드 배치 허용
* 아희 특유의 공간 기반 코드 스타일 대응 가능
* VM 실행 안정성 향상

효과:

* 코드와 장식 요소를 동일 공간에 배치 가능
* 난해 언어 특유의 “공간적 흐름 표현” 지원 기반 확보

---

### 스택 길이 확인 함수(len) 구현

문제:

* 명령 처리 과정에서 매번 현재 활성 스택 길이를 직접 참조해야 했음
* 스택 언더플로우 검사 코드가 반복적으로 등장하고 있었음

원인:

* 저장공간 접근 로직이 각 명령 내부에 분산되어 있었음
* 현재 활성 스택 상태를 추상화하는 인터페이스가 부족했음

해결:

* 현재 활성 스택 길이를 반환하는 `len()` 함수 구현

구조:

* `stacks.get(window.active)` 기반 참조
* 현재 활성 저장공간 길이 반환

결과:

* 스택 상태 접근 단순화
* 명령 처리 코드 가독성 향상
* 이후 언더플로우 처리 함수화 기반 마련

효과:

* 저장공간 접근 추상화 단계 진입
* VM 명령 처리 로직 유지보수 효율 증가

---

### 방향 반전(flip) 함수 구현

문제:

* 스택 부족 상황 발생 시 방향 반전 로직이 여러 위치에서 반복되고 있었음
* 방향 전환 코드가 직접 삽입되며 실행 흐름 가독성이 저하되고 있었음

원인:

* 방향 반전이 독립 기능으로 분리되지 않은 상태였음

해결:

* 방향 벡터를 반전시키는 `flip()` 함수 구현

구조:

* `dir.x *= -1`
* `dir.y *= -1`

결과:

* 방향 반전 처리 통합
* 명령 실패 시 제어 흐름 표현 간결화
* 실행 흐름 추적 난이도 감소

효과:

* 방향 기반 제어 흐름 구조 명확화
* 이후 조건 실패 처리 및 예외 흐름 확장 용이성 확보

---

### 복제 명령(ㅃ) 구현

문제:

* 기존 VM은 기본 연산 및 입출력 명령만 구현된 상태였으며,
  아희 특유의 스택 조작 명령 일부가 누락되어 있었음

해결:

* `ㅃ` 명령 구현
* 스택 top 요소 복제 기능 추가

동작:

* 스택이 비어있을 경우:

  * 이동 방향 반전
* 정상 상태일 경우:

  * top 요소 pop
  * 동일 값 2회 push

구조:

* `len()` 기반 스택 상태 검사
* `flip()` 기반 실패 처리 연동

결과:

* 스택 조작 명령 체계 확장
* 실제 아희 프로그램 호환성 증가
* VM 명령 실행 범위 확대

효과:

* 저장공간 기반 연산 표현력 향상
* 이후 교환(ㅍ), 비교(ㅈ) 등 명령 구현 기반 강화

---

### 구조 개선 결과

현재 인터프리터는 다음 요소를 추가적으로 확보하였다.

추가 구성 요소:

* 비한글 문자 skip 처리 구조
* 저장공간 길이 추상화 함수
* 방향 반전 추상화 함수
* 스택 복제 명령(ㅃ)

현재 상태:

* 단순 명령 실행 수준을 넘어
* 공간 기반 제어 흐름과 예외 흐름을 처리 가능한 VM 구조로 발전
* 저장공간 상태와 이동 흐름을 안정적으로 제어 가능한 단계에 도달

## 7. 개발 일지 (2026-06-03)

### 비교 명령(ㅈ) 구현

문제:

* 기존 VM은 조건 분기에 필요한 비교 연산을 지원하지 않았음
* 스택 기반 조건 판단이 불가능하여 일부 아희 프로그램 실행에 제약이 존재했음

원인:

* 비교 결과를 저장공간에 반환하는 명령이 구현되지 않은 상태였음

해결:

* `ㅈ` 명령 구현
* 스택 top 2개를 비교하여 결과 반환

동작:

* 저장공간 길이가 2 미만일 경우:

  * 이동 방향 반전
* 정상 상태일 경우:

  * 값 2개 pop
  * `b >= a` 판별
  * 참이면 `1`
  * 거짓이면 `0`
  * 결과 push

구조:

* `len()` 기반 스택 상태 검사
* `flip()` 기반 실패 처리

결과:

* 조건 분기 구현 기반 확보
* 비교 연산 수행 가능
* 실제 아희 프로그램 호환성 증가

효과:

* 조건 흐름 표현력 향상
* 분기 구조 구현 가능

---

### 저장공간 이동 명령(ㅆ) 구현

문제:

* 저장공간 간 값 이동 기능이 부재하였음
* 다중 저장공간을 활용하는 프로그램 실행이 불가능했음

원인:

* 현재 활성 저장공간 외부에 값을 전달하는 구조가 존재하지 않았음

해결:

* `ㅆ` 명령 구현
* 현재 저장공간의 값을 지정 저장공간으로 이동

동작:

* 저장공간이 비어있을 경우:

  * 이동 방향 반전
* 정상 상태일 경우:

  * 값 pop
  * 대상 저장공간 선택
  * 값 push

구조:

* 현재 활성 저장공간 백업
* 대상 저장공간 임시 전환
* `try-finally` 기반 상태 복구

결과:

* 저장공간 간 데이터 전달 가능
* 다중 스택 활용 가능

효과:

* 저장공간 구조 활용도 향상
* 복잡한 프로그램 실행 기반 확보

---

### 교환 명령(ㅍ) 구현

문제:

* 스택 top 요소 교환 기능이 구현되지 않았음
* 일부 알고리즘성 아희 코드 실행에 제약이 존재했음

해결:

* `ㅍ` 명령 구현
* top 2개 요소 위치 교환

동작:

* 저장공간 길이가 2 미만일 경우:

  * 이동 방향 반전
* 정상 상태일 경우:

  * 값 2개 pop
  * 역순 push

구조:

* `len()` 기반 상태 검사
* `flip()` 기반 실패 처리

결과:

* 스택 재배열 가능
* 저장공간 조작 범위 확대

효과:

* 명령 조합 표현력 증가
* 알고리즘 구현 유연성 향상

---

### 조건 명령(ㅊ) 구현

문제:

* 조건적 방향 전환 명령이 부재하였음
* 아희의 핵심 분기 구조를 사용할 수 없었음

해결:

* `ㅊ` 명령 구현

동작:

* 저장공간이 비어있을 경우:

  * 이동 방향 반전
* 정상 상태일 경우:

  * 값 pop
  * 값이 0이면 방향 반전
  * 0이 아니면 진행 유지

구조:

* `len()` 기반 검사
* `flip()` 기반 제어

결과:

* 조건 분기 기능 확보
* 프로그램 흐름 제어 가능

효과:

* 제어 흐름 표현력 대폭 향상
* 실제 아희 프로그램 실행 가능성 증가

---

### 에디터 복사·붙여넣기 기능 구현

문제:

* 코드 입력 시 개별 셀 단위 편집만 가능하였음
* 외부 아희 코드 복사 및 테스트가 어려웠음

원인:

* 붙여넣기 이벤트가 처리되지 않고 있었음

해결:

* Clipboard API 기반 붙여넣기 기능 구현
* 다중 문자 입력 처리 구조 추가

구조:

* `paste` 이벤트 처리
* 줄바꿈 인식
* 2차원 그리드 자동 배치

결과:

* 외부 코드 즉시 테스트 가능
* 코드 작성 속도 향상

효과:

* 에디터 실사용성 개선
* 디버깅 및 테스트 효율 향상

---

### 출력창 시스템 구현

문제:

* 프로그램 출력이 콘솔에만 표시되고 있었음
* 사용자 인터페이스 내에서 결과 확인이 불가능했음

해결:

* 전용 출력 패널 추가
* 출력 버퍼 기반 렌더링 구조 적용

구조:

* 출력 문자열 누적
* DOM 출력 영역 연동

결과:

* 실행 결과 실시간 확인 가능
* IDE 형태의 사용 환경 확보

효과:

* 사용자 경험 개선
* 디버깅 편의성 향상

---

### 스택 패널 개선

문제:

* 저장공간 데이터 증가 시 패널 길이가 과도하게 확장되었음
* 인터페이스 가독성이 저하되었음

해결:

* 스크롤 기반 표시 구조 적용

구조:

* 최대 높이 제한
* `overflow-y` 기반 스크롤 처리

결과:

* 대형 스택 데이터 관리 가능
* 화면 공간 활용도 향상

효과:

* UI 안정성 향상
* 장시간 디버깅 환경 개선

---

### 구조 개선 결과

현재 인터프리터는 다음 요소를 추가적으로 확보하였다.

추가 구성 요소:

* 비교 명령(ㅈ)
* 저장공간 이동 명령(ㅆ)
* 교환 명령(ㅍ)
* 조건 명령(ㅊ)
* 복사·붙여넣기 기능
* 출력창 시스템
* 스크롤 기반 스택 패널

현재 상태:

* 아희 핵심 명령어 구현 완료
* 저장공간 제어 기능 확보
* 조건 분기 구조 구현 완료
* 기본 IDE 기능 구축 시작
* 실제 프로그램 실행 및 디버깅 가능한 단계 도달

---

## 8. 개발 일지 (2026-06-05)

### 반사 명령(ㅣ, ㅡ, ㅢ) 구현

문제:

* 일부 아희 프로그램이 예상과 다른 경로로 실행되고 있었음
* 진행 방향을 제어하는 반사 계열 명령이 누락되어 있었음

원인:

* 방향 전환 명령 중 `ㅣ`, `ㅡ`, `ㅢ`가 구현되지 않은 상태였음

해결:

* `ㅣ`, `ㅡ`, `ㅢ` 명령 구현

동작:

* `ㅣ`

  * 상하 이동 시 진행 유지
  * 좌우 이동 시 방향 반전

* `ㅡ`

  * 좌우 이동 시 진행 유지
  * 상하 이동 시 방향 반전

* `ㅢ`

  * 현재 진행 방향 완전 반전

구조:

* 방향 벡터 기반 처리
* 이동 방향 상태 직접 수정

결과:

* 반사 계열 명령 구현 완료
* 방향 제어 기능 정상 동작

효과:

* 실제 아희 프로그램 호환성 향상
* 제어 흐름 정확도 개선

---

### 에디터 블록 선택 기능 구현 시작

문제:

* 코드 편집 시 단일 셀 단위 조작만 가능하였음
* 다수 셀을 한 번에 처리할 수 없었음

원인:

* 선택 영역 개념이 에디터에 존재하지 않았음

해결:

* 마우스 드래그 기반 블록 선택 구조 설계
* 선택 영역 렌더링 기능 추가

동작:

* 마우스 클릭 시 선택 시작점 기록
* 드래그 중 선택 종료점 갱신
* 시작점과 종료점을 기준으로 직사각형 범위 생성
* 선택 영역 실시간 표시

구조:

* 선택 상태 플래그 추가
* 시작 좌표·종료 좌표 저장
* 범위 판정 함수 구현
* 렌더링 단계에서 선택 영역 시각화

결과:

* 다중 셀 선택 기능 기반 확보
* 영역 단위 편집 기능 구현 준비 완료

효과:

* 복사·붙여넣기 기능 확장 기반 확보
* 에디터 사용성 향상
* IDE 수준 편집 기능 구현 가능성 확보

---

### 구조 개선 결과

현재 인터프리터 및 IDE는 다음 요소를 추가적으로 확보하였다.

추가 구성 요소:

* 반사 명령(ㅣ)
* 반사 명령(ㅡ)
* 반전 명령(ㅢ)
* 블록 선택 기능 기반

현재 상태:

* 아희 핵심 명령어 구현 사실상 완료
* 방향 제어 계열 명령 구현 완료
* 저장공간 및 분기 명령 구현 완료
* IDE 편집 기능 확장 진행 중
* 실제 프로그램 작성·실행·디버깅 가능한 단계 도달

향후 계획:

* 블록 복사 기능
* 블록 삭제 기능
* 블록 잘라내기 기능
* 블록 이동 기능
* 파일 저장·불러오기 기능
* 디버거 기능 고도화

---

# 8. 개발 일지 (2026-06-06)

## 자동 확장 에디터 구현

### 문제
- 기존 에디터는 고정 크기(20×10)만 지원
- 대형 아희 프로그램 입력 시 공간 부족 발생

### 원인
- 그리드 크기가 초기 생성 시점에 고정됨
- 편집 중 동적 크기 증가 기능 부재

### 해결
- 열 확장 함수 `expandWidth()` 구현
- 행 확장 함수 `expandHeight()` 구현
- 커서 이동 및 붙여넣기 과정에 자동 확장 로직 적용

### 동작 구조
- 마지막 열 입력 시 → 열 자동 증가
- 마지막 행 아래 이동 시 → 행 자동 증가
- 대량 붙여넣기 시 → 필요한 만큼 동적 확장

### 내부 구조
- `window.W`
- `window.H`
- `board.push()`
- `board[y].push()`

### 결과
- 사실상 무제한 크기 편집기 구현
- 대형 아희 프로그램 입력 지원

### 효과
- 실사용 가능한 코드 에디터 기반 확보
- 복잡한 아희 프로그램 작성 가능

---

## 드래그 기반 영역 선택 기능

### 문제
- 단일 셀 선택만 가능
- 다중 셀 조작 불가

### 해결
- 마우스 드래그 기반 선택 시스템 구현

### 동작
- 마우스 클릭 → 선택 시작
- 드래그 → 선택 영역 갱신
- 마우스 업 → 선택 종료

### 내부 구조
- `selectStartX`
- `selectStartY`
- `selectEndX`
- `selectEndY`
- `isSelected()`

### 결과
- 직사각형 영역 선택 지원

### 효과
- 복사/잘라내기 기능 기반 확보
- 에디터 UX 향상

---

## 스크롤 기반 에디터 구조 적용

### 문제
- 자동 확장 시 UI 패널과 충돌
- 레이아웃 붕괴 발생

### 원인
- 무제한 확장 구조
- 표시 영역 제한 없음

### 해결
- `editor_container` 구조 추가
- 스크롤 기반 렌더링 적용

### 구조
- `overflow: auto`
- `width: max-content`
- `height: max-content`

### 결과
- 대형 코드 편집 안정화
- UI 구조 붕괴 해결

### 효과
- 장시간 작업 환경 개선
- 대규모 프로그램 대응 가능
- 스크롤 UX 확보

---

## 커스텀 스크롤바 적용

### 문제
- 기본 브라우저 스크롤바 UI 불일치

### 해결
- WebKit 기반 커스텀 스크롤바 적용

### 구조
- `::-webkit-scrollbar`
- `::-webkit-scrollbar-thumb`
- `::-webkit-scrollbar-track`

### 결과
- 에디터 및 패널 UI 통일

### 효과
- 시각적 일관성 확보
- UI 완성도 향상

---

## 99 Bottles of Beer 디버깅

### 문제
- `"on the wall"` 출력 단계에서 무한 루프 발생

### 원인 조사
- 아희 구현체 상태 비교 분석
- 특정 시점 스택 불일치 확인

### 핵심 원인
- `ㅂ` 명령 구현 중 `ㅇ` 처리 누락
- 숫자 상수 `0` 생성 기능 미구현

### 해결
- `ㅂ + ㅇ` 조합 → `0 push` 구현 추가

### 결과
- 스택 상태 reference implementation과 일치
- 무한 루프 제거

### 효과
- 대형 프로그램 호환성 개선
- 명세 준수 수준 향상

---

## 구조 개선 결과

### 현재 확보된 구성 요소
- 자동 행 확장
- 자동 열 확장
- 드래그 기반 영역 선택
- 스크롤 기반 에디터
- 커스텀 스크롤바
- `ㅂ + ㅇ` 상수 생성 수정
- 99 Bottles of Beer 디버깅 완료

---

## 현재 상태

- 아희 핵심 명령어 구현 완료
- 대형 예제 실행 가능
- 동적 크기 에디터 구축 완료
- IDE 기능 확장 단계 진입
- 실제 아희 프로그램 호환성 검증 단계

---